# 2026-07-27 DEV 部署記錄（CHANGE-107 + FIX-133）

> **部署人**: AI 助手（使用者授權）
> **環境**: Azure DEV（App Service for Containers）
> **映像**: `acrscmdocprocessingdev.azurecr.io/ai-document-extraction:dev-change107-fix133-20260727115120`
> **對應 commit**: `origin/main` @ `fe69379`（#148 合併後）
> **前一版映像**: `dev-fix131-132-20260723111721`（對應 `38fbd4e` 基準）

---

## 本次帶上的變更

線上映像停在 7/23，`main` 已往前 **8 個 commit**。其中**只有 2 項影響映像**，其餘 6 個為文件／一次性腳本／skill —— `.dockerignore` 排除 `claudedocs`、`*.md`、`scripts/local-*`，完全不進 build context。

| 項目 | 內容 |
|------|------|
| CHANGE-107（#144） | 模版欄位映射列表加「複製」動作 → 導向 `new?copyFrom=<id>` 預填全部規則／說明／優先級／啟用狀態，名稱加「（複製）」後綴，四個身分欄位（`dataTemplateId` / `scope` / `companyId` / `documentFormatId`）一律清空強制重選。含三項防護：`TargetFieldSelector` 無效欄位視覺標示＋儲存前擋下、模版未選時揭露「已複製 N 條規則」、POST 唯一衝突判定由 `error.message.includes('unique')` 字串比對改為 `error.code === 'P2002'`。另加 `service.create()` 應用層四元組重複檢查（→ 409） |
| FIX-133（#147） | `schema.prisma` 移除失效的 `@@unique([dataTemplateId, scope, companyId, documentFormatId])` —— 唯一性改由 DB 端部分唯一索引 `template_field_mappings_active_unique`（`NULLS NOT DISTINCT WHERE is_active = true`）保證。Dockerfile 於 `init.sql` 生成後追加 `cat prisma/post-init-indexes.sql`，確保**全新空庫**也有唯一性保護 |

### 不影響映像的 6 個 commit

`fc47e17`（7/23 部署記錄）、`2b705fa`（`/restart-services` skill）、`31e2ab8`+`b2ba4d0`+`aa17183`（CHANGE-107 / FIX-133 規劃與盤點文件）、`fe69379`（CHANGE-108 的 Azure→本地同步腳本，屬 `scripts/local-*` 與 `prisma/export-*`，entrypoint 未引用）。

## 部署前檢查

依 runbook §A.0 + 兩次事故教訓（§16 新 env 未同步、memory `feedback_azure_migration_needs_schema_drift_entry`）。

| 檢查項 | 結果 |
|--------|------|
| 映像落後範圍（`38fbd4e..origin/main`） | 8 個 commit，逐一分類確認無其他功能變更混入 ✅ |
| 新增／改名 env | **0 個** —— `.env.example` 無改動；另直接掃該區間全部改動的 `src/` 檔案，`process.env` 引用為 **0**。兩路互相印證，避開 §16「靜默 fallback → 整批 `OCR_FAILED`」的成因 ✅ |
| 新依賴 | **無** —— `package.json` / `package-lock.json` 未改 ✅ |
| Prisma migration | 有 1 個（`20260725060000_fix133_template_mapping_partial_unique_index`），但該索引已於 **2026-07-25 經 Kudu 直接套用並就地驗證** → **不需** `RUN_SCHEMA_DRIFT_FIX` ✅ |
| `RUN_*` 一次性旗標 | 全部 `false`。`RUN_AZURE_SYNC=inspect` 仍存在，但 `origin/main` 的 entrypoint 無對應分支 → 惰性設定，且 inspect 本為唯讀 ✅ |
| `scripts/docker-entrypoint.sh` 行尾 | LF（0 個 CRLF）→ 避開 §12 容器 exit 127 ✅ |
| i18n 同步 | `npm run i18n:check` exit 0；`templateFieldMapping.json` 三語言對稱改動 ✅ |
| 資料安全評估 | 庫非空 → bootstrap 走 skip-init（無 DDL）；essential seed 冪等 upsert；無一次性腳本 → **無資料遺失／覆蓋風險** ✅ |
| 部署前健康基準 | `HTTP 200`、`healthy`、`database: connected`、uptime 2185s ✅ |

## 執行步驟

| # | 步驟 | 結果 |
|---|------|------|
| 1 | `az acr build` → tag `dev-change107-fix133-20260727115120` | ✅ run `ck1a` **Succeeded**（08:02，56 步 —— 比 FIX-081 的 55 步多一步，正是新增的 `cat post-init-indexes.sql`）。本機命令 exit 1 = runbook §A.1 記載的 cp1252 log 串流崩潰（`✔` 於 Step 19 `npx prisma generate`），**不影響雲端建置** |
| 2 | tag 就緒確認 | ✅ `repository show-tags` 最新一筆即為新 tag |
| 3 | `az webapp config container set` | ✅ 回傳值核驗 `DOCKER_CUSTOM_IMAGE_NAME` 已切換 |
| 4 | `az webapp restart` | ✅ `04:04:34Z` |
| 5 | 健康輪詢 | ✅ `04:06:02Z`（距重啟 **88 秒**）、`uptime 16.4s` → 確證為**新 instance**，非舊 worker 冒充 200 |
| 6 | 容器 log 驗證（ARM `containerlogs` + Log Analytics `AppServiceConsoleLogs`） | ✅ 見下節 |
| 7 | CHANGE-107 功能驗收（Playwright） | ✅ 見下節 |

> **`uptime` 是必要的判定依據**，不是多餘的嚴謹。7/27 早前設 `CRON_SECRET` 時踩過：app setting 改完 `/api/health` 立刻回 200，但那是尚未載入新環境變數的舊 worker。只看 200 會誤判部署已生效。

## 容器 log 關鍵行

```
[entrypoint] Step 1/3: bootstrap database schema (if needed)      04:05:42
[bootstrap] public schema already has 122 tables -> skip init.sql  04:05:42   ← 無 DDL，證實無資料風險
[entrypoint] Step 2/3: run essential seed (idempotent)             04:05:42
 Essential seed completed in 2.2s                                  04:05:45
[entrypoint] (optional) template field mapping seed: mode=false    04:05:45   ← gated 旗標關閉
[entrypoint] Step 3/3: starting Next.js server                     04:05:45
  Ready in 471ms                                                   04:05:46
```

平台事件：新映像 `04:05:36` 拉取 → 容器 `a42f488fe19c` `04:05:42` 建立 → `WarmUpProbeSucceeded`（5.0s）→ `Site started` `04:05:47`。

**錯誤掃描 0 命中**：`exec: ... not found`（§12 CRLF）、`re2.wasm` ENOENT（§13）、`MODULE_NOT_FOUND`、`P2028`、`P2022`、`P2002`、`Unhandled`、`FATAL`。

> log 中 `SiteStartupCancelled @ 04:04:37` 是重啟時舊容器被停止的**預期產物**，以 `LastError` 欄位在後續事件中延續顯示，非新故障 —— 其後所有動作皆成功。

## CHANGE-107 功能驗收（Azure 實機，2026-07-27）

| 驗收項 | 實測 |
|---|---|
| 列表出現「複製」動作 | ✅ 每列動作由部署前的「編輯」「刪除」變為「編輯」「**複製**」「刪除」（共 36 個配置） |
| 導向與預填 | ✅ 點擊 → `/new?copyFrom=cmrwu7bqb001101miqgc5e989`、頁標題「複製映射配置」 |
| 規則條數揭露（防護 2） | ✅ 提示「已從『CEVA LOGISTICS (HONG KONG) LTD - Inbound Template (Full List)』複製 **10 條規則**。請選擇數據模版與範圍後儲存。」—— 與列表「規則數 10」一致；規則區顯示「請先選擇數據模版」，條數已在提示中揭露，不會讓使用者以為規則遺失 |
| 名稱後綴 | ✅ `CEVA LOGISTICS (HONG KONG) LTD - Inbound Template (Full List)（複製）` |
| 四個身分欄位清空 | ✅ 數據模版顯示「選擇數據模版」、範圍顯示「選擇範圍」→ 強制重選 |
| 說明／優先級／啟用狀態沿用 | ✅ 優先級 `0`、啟用開關已勾 |
| i18n | ✅ 全繁體中文、無缺 key、無 raw key 外露 |

> **未按「建立」** —— 純讀取驗證，不在使用者測試環境產生資料。故「實際建立複製記錄並確認 409 擋阻」一項未涵蓋，留給使用者實際使用時自然驗證。
>
> 唯一 console error 為 `/favicon.ico` 404 —— 既有的裝飾性問題，與本次部署無關。

## 附帶取得：FIX-133 資料修正的獨立佐證

驗收時擷取的列表意外印證了 FIX-133 於 7/25 經 Kudu 執行的資料修正（當時僅由腳本自報 + 重跑盤點腳本確認，未經 UI）：

| 記錄 | 列表顯示 | 與 FIX-133 記載一致？ |
|---|---|---|
| `cmrwu7bqb0` CEVA Inbound（保留筆） | 規則數 **10**、**啟用** | ✅ B-1 改 4 條規則後仍為 10 條 |
| `cmrimxy970` CEVA Inbound（舊筆） | 規則數 7、**停用** | ✅ B-1 已將其停用 |
| `cmrin1af90` CEVA Outbound | 規則數 **9**、啟用 | ✅ Outbound 補齊 5 → 9 |

仍未涵蓋：逐條公式內容的 UI 檢視、實際跑一次 template 匹配核對金額（FIX-133 的「（建議）」項）。

## 回滾方式

```bash
az webapp config container set -g RG-RAPOSCM-AIDocProcessing-DEV -n WebApp-RAPOSCM-AIDocProcessing-DEV \
  --container-image-name acrscmdocprocessingdev.azurecr.io/ai-document-extraction:dev-fix131-132-20260723111721
az webapp restart -g RG-RAPOSCM-AIDocProcessing-DEV -n WebApp-RAPOSCM-AIDocProcessing-DEV
```

本次無 schema DDL、無新 env、無一次性旗標 → 回滾＝重部署舊映像即可，無需額外還原步驟。

> ⚠️ 唯一需注意：回滾後 Prisma Client 會退回含 `@@unique` 的舊版。因**無任何程式碼使用該複合鍵**（FIX-133 已全庫確認無 `findUnique` / `upsert` 用到），不影響運行；DB 端的部分唯一索引也不受映像回滾影響（它是 7/25 直接套用的 DDL）。

## 本次部署後的狀態變化

| 項目 | 部署前 | 部署後 |
|---|---|---|
| Prisma Client 與 DB 索引一致性 | 不一致（Client 含 `@@unique`，DB 已是部分索引） | **一致** —— FIX-133 的收尾 |
| 全新空庫的唯一性保護 | 無（`init.sql` 不含部分索引） | **有** —— Dockerfile 追加 `post-init-indexes.sql` |
| 模版欄位映射複製功能 | 無 | **可用** |

## 仍未處理（非本次部署範圍）

1. 🔴 **FIX-094 的 sweeper 排程仍未設定** —— `CRON_SECRET` 已於 7/27 打通途徑，但無任何排程器呼叫 `POST /api/jobs/stuck-processing-sweeper`。未設之前卡住文件會再度累積
2. **FIX-134**（全域管理員識別三套並存）尚未實作 —— 全域管理員目前仍無法從 UI／API 直接觸發 sweeper 與 `/api/audit/reports`（皆 403）
3. `RUN_AZURE_SYNC=inspect` 建議一併設回 `false`（目前為惰性，但依 runbook §A.5「一次性旗標用完即設回 false」的紀律應清理）
4. FIX-133 的「（建議）」項：template 匹配金額端到端核對
