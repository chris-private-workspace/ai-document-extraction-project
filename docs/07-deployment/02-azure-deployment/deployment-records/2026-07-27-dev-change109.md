# 2026-07-27 DEV 部署記錄（CHANGE-109）

> **部署人**: AI 助手（使用者授權）
> **環境**: Azure DEV（App Service for Containers）
> **映像**: `acrscmdocprocessingdev.azurecr.io/ai-document-extraction:dev-change109-20260727143423`
> **對應 commit**: `origin/main` @ `3a6656f`（#151 合併後）
> **前一版映像**: `dev-change107-fix133-20260727115120`（對應 `def6a0a`）
> **同日第二次部署**（上午為 CHANGE-107 + FIX-133，見 [該記錄](2026-07-27-dev-change107-fix133.md)）

---

## 本次帶上的變更

線上映像僅落後 **1 個 commit** —— 早上剛部署過，之後只合併了 CHANGE-109。

| 項目 | 內容 |
|------|------|
| CHANGE-109（#151）| Template Instance 過期偵測擴充：偵測「同一發票（同 `companyId` + 同 `invoice_number`）另有處理時間較晚的 document 記錄」。含 `ExtractionResult.invoiceNumber` 新欄位 + `(companyId, invoiceNumber)` 索引、提取時反正規化寫入、`getRows` 單一批次候選查詢、列表天藍 badge + 抽屜警示、i18n 三語言、10 項單元測試 |

補上 [CHANGE-106](../../../../claudedocs/4-changes/feature-changes/CHANGE-106-template-instance-staleness-indicator.md) 涵蓋不到的情境 —— 其判斷式只在**同一筆** document 被原地重新處理時才亮，而此環境的實際工作流是**重新上傳**（產生新記錄）。

## 部署前檢查

| 檢查項 | 結果 |
|--------|------|
| 映像落後範圍（`def6a0a..HEAD`）| **1 個 commit**（僅 CHANGE-109），無其他變更混入 ✅ |
| 新增／改名 env | **0 個** —— `.env.example` 無改動；另掃該區間全部改動的 `src/` 檔案，`process.env` 引用為 0 ✅ |
| Prisma 變更 | 有新欄位 + 索引 → **必須**帶 `RUN_SCHEMA_DRIFT_FIX`（與上午那次不同）✅ |
| 回填需求 | 有 → **必須**帶一次性 `RUN_INVOICE_NUMBER_BACKFILL` ✅ |
| `scripts/docker-entrypoint.sh` 行尾 | 工作樹 0 CRLF（`.gitattributes` `*.sh text eol=lf` 保護）✅ |
| 新腳本 `prisma/backfill-invoice-number.js` | 工作樹有 CRLF（`core.autocrlf=true` 於 checkout 轉換），但 **git 儲存為 LF**，且 `.js` 的 CRLF 對 Node 無影響 ✅ |
| 工作樹 | 乾淨，於 `main` @ `3a6656f` ✅ |
| 部署前健康基準 | `HTTP 200`、`healthy`、`database: connected`、uptime 8918s ✅ |

## 執行步驟

| # | 步驟 | 結果 |
|---|------|------|
| 1 | `az acr build` → tag `dev-change109-20260727143423` | ✅ run `ck1b` **Succeeded**（08:22）。本機 exit 1 = runbook §A.1 記載的 cp1252 串流崩潰 |
| 2 | tag 就緒確認 | ✅ `repository show-tags` 最新一筆 |
| 3 | **A.2 設兩個 gated 旗標** | ✅ `RUN_SCHEMA_DRIFT_FIX=true`、`RUN_INVOICE_NUMBER_BACKFILL=true`（設定變更本身觸發了一次重啟，起的是**舊映像**容器 —— 見下方「一個判讀更正」）|
| 4 | `az webapp config container set` | ✅ 回傳值核驗已切換 |
| 5 | `az webapp restart` | ✅ `06:46:58Z` |
| 6 | 新容器接手 | ✅ 映像 `06:48:04` 拉取 → 容器 `733ebdc6cd54` 啟動 → `WarmUpProbeSucceeded`（19.4s）→ `Site started` `06:48:24` → 健康端點 `uptime 16.5s` @ `06:48:25` |
| 7 | 功能驗收（Playwright）| ✅ 見下 |
| 8 | **A.5 旗標設回 `false`** | ✅ 兩者皆已核驗為 `false` |

## 兩個一次性作業的實際輸出

```
[entrypoint] Step 1/3: bootstrap database schema (if needed)     06:48:04
[bootstrap] public schema already has 122 tables -> skip init.sql 06:48:05
[entrypoint] (optional) applying schema drift fixes               06:48:05
[schema-drift] OK CHANGE-109 column extraction_results.invoice_number
[schema-drift] OK CHANGE-109 index extraction_results (company_id, invoice_number)
[schema-drift] done — 11 applied, 0 failed                       06:48:05
[entrypoint] Step 2/3: run essential seed (idempotent)            06:48:05
 Essential seed completed in 2.1s                                06:48:07
[entrypoint] (optional) template field mapping seed: mode=false   06:48:07
[entrypoint] (optional) applying CHANGE-109 invoice_number backfill 06:48:08
[backfill-invoice] mode=APPLY
[backfill-invoice] rows with null invoice_number: 550
[backfill-invoice] batch done — scanned=500 wouldFill=437 noInvoiceNumber=63
[backfill-invoice] batch done — scanned=550 wouldFill=483 noInvoiceNumber=67
[backfill-invoice] filled 483 row(s); 67 row(s) have no usable invoice_number
[backfill-invoice] rows with invoice_number now: 483              06:48:09
[entrypoint] Step 3/3: starting Next.js server                   06:48:09
  Ready in 446ms                                                 06:48:10
```

**`11 applied` 是關鍵數字** —— 舊映像的 drift 清單只有 9 條（止於 FIX-133），多出的 2 條正是 CHANGE-109 的欄位與索引。

回填 550 筆候選中填了 **483 筆（88%）**；67 筆無可用發票號（提取失敗類文件：`UPLOADED` / `OCR_FAILED` / `REF_MATCH_FAILED`），按設計留 null、不參與同發票比對。

**錯誤掃描 0 命中**：`exec: ... not found`（§12 CRLF）、`re2.wasm` ENOENT（§13）、`MODULE_NOT_FOUND`、`P2028`、`P2022`、`P2002`、`schema-drift ... FAIL`、`backfill ... failed`、`Unhandled`、`FATAL`。

## 功能驗收（Azure 實機，2026-07-27）

驗證目標為 CHANGE-109 §驗收標準 第 2、3 項指定的具體實例：7/14 的 `cmrkc7ajs000001qrgyp2jzxs`。

| 驗收項 | 實測 |
|---|---|
| 欄位存在於 API | ✅ 該實例 10 個 row 全帶 `newerVersions` |
| **目標列命中** | ✅ `RCIM250325`（來源 `CEVA_RCIM250325_17865.PDF`）列出 **8 筆**更新版本，`invoiceNumber` 全為 `F260017865` |
| 含手動確認過的兩筆 | ✅ `7c3e3981`（7/21 10:19:19.990Z）與 `d534a63f`（7/23 04:23:03.882Z）皆在清單內 |
| 列表 badge | ✅ 天藍色「有更新版本」+ `Files` 圖示（`border-sky-500/60 bg-sky-50 text-sky-700`）|
| **兩訊號獨立** | ✅ 這些列**無琥珀 badge** —— 來源文件本身沒被原地重新處理，只是同發票另有更新記錄。證實兩種訊號互不干擾 |
| 抽屜下鑽 | ✅ 完整列出 8 筆檔名 + 處理時間，依時間新到舊排序 |
| 時區換算 | ✅ 7/23 04:23 UTC → 顯示 12:23；7/21 10:19 → 18:19；7/15 09:25 → 17:25（皆 UTC+8）|
| i18n | ✅ 全繁體中文、無缺 key |

### 實際筆數比規劃預期多，差額已交代

CHANGE-109 §驗收標準 第 3 項寫「預期 6 筆，少於 6 需查明原因」。**實測 8 筆**。

多出的 2 筆是 `CEVA_RCIM250325_17865 1.PDF` 的 `2026-07-16T07:22:28Z` 與 `2026-07-15T09:25:10Z` —— 兩者確實晚於本列的 `updatedAt`（`2026-07-14T07:36:04Z`），符合判斷式。**是規劃時目視清單漏數，不是程式多報。**

### 附帶觀察：該實例 10 個 row 全部被標記

7/14 的實例，其 10 份來源文件之後全都被重新上傳過。逐列的更新版本數為 1／2／3／**8**／3／5／1／10／1／2。這正是 CHANGE-106 驗證時「522 個 row 0 個被標記」的另一面 —— 過期是普遍現象，只是舊判斷式偵測不到。

## 對 Azure 的實際改動

| 項目 | 說明 |
|------|------|
| 映像切換 | → `dev-change109-20260727143423` |
| `extraction_results` 加欄位 + 索引 | `invoice_number text` + `extraction_results_company_id_invoice_number_idx` |
| 資料寫入 | **483 筆** `extraction_results.invoice_number` 由 null 填為實際發票號（值來自同筆記錄的 `field_mappings` JSON，非外部來源）|
| app settings | 新增 `RUN_INVOICE_NUMBER_BACKFILL`；`RUN_SCHEMA_DRIFT_FIX` 由 false→true→false。**兩者收尾皆為 `false`** |
| 重啟次數 | 3 次（旗標設定 → 映像切換 → 旗標歸零）|

> 回填只寫 `invoice_number` 這一個新欄位、且只寫原本為 null 的列，不觸碰任何既有欄位。

## 一個判讀更正（值得記錄）

執行中我一度依「健康端點回 200」判定新容器已接手，實際是**舊 worker**：

```
restart 發出       06:46:58Z
健康端點回 200     06:47:08Z   uptime = 347s  ← 距重啟僅 10 秒，不可能是新容器
真正的新容器       06:48:25Z   uptime = 16.5s
```

`uptime 347s` 對應 `06:41:21` 啟動的容器 —— 那是**步驟 3 設定旗標時**觸發的重啟，跑的仍是舊映像（其 drift 只有 9 條、且無回填區塊，log 可證）。

改為輪詢到 `uptime < 200` 才確認接手。**只看 HTTP 200 會誤判部署已生效** —— 這與上午 CHANGE-107 部署記錄的同一項教訓一致，兩次都靠 `uptime` 才判對。

## 回滾方式

```bash
az webapp config container set -g RG-RAPOSCM-AIDocProcessing-DEV -n WebApp-RAPOSCM-AIDocProcessing-DEV \
  --container-image-name acrscmdocprocessingdev.azurecr.io/ai-document-extraction:dev-change107-fix133-20260727115120
az webapp restart -g RG-RAPOSCM-AIDocProcessing-DEV -n WebApp-RAPOSCM-AIDocProcessing-DEV
```

回滾**不需**還原欄位、索引或回填資料：

- 新欄位為 nullable、舊程式碼不讀它 → 留著無影響
- 索引不影響舊查詢
- 483 筆 `invoice_number` 的值是從同筆記錄的 JSON 抄來的副本，舊程式碼不使用

## 仍未處理（非本次部署範圍）

1. 🔴 **FIX-094 的 sweeper 排程仍未設定** —— `CRON_SECRET` 已打通途徑，但無任何排程器呼叫 `POST /api/jobs/stuck-processing-sweeper`。未設之前卡住文件會再累積
2. **FIX-134**（全域管理員識別三套並存）未實作 —— 全域管理員仍被 sweeper 與 `/api/audit/reports` 兩個端點 403
3. `RUN_AZURE_SYNC=inspect` 建議設回 `false`（對現行映像為惰性設定，但依 §A.5 紀律應清理）
4. **`.gitattributes` 只保護 `*.sh`** —— CHANGE-109 開發期間有 6 個程式碼檔 + 1 個文件檔被編輯工具翻成 CRLF，造成誤導性的巨大 diff（`template-instance.service.ts` 1160/1021 vs 實際 ~160 行）。建議加 `* text=auto eol=lf` 從源頭擋掉
5. CHANGE-109 的後續選項：mapping 設定被修改也視為過期依據（原 §建議行為 第 4 項，選用未實作）；以及「一鍵改用新文件重建本列」（CHANGE-106 §方案選項 B，需先釐清人工修正值的覆蓋語意）
