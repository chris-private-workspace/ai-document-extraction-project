# DEV 部署記錄：CHANGE-103 P2 + FIX-111/112/113（2026-07-16）

> **環境**: Azure DEV
> **映像 tag**: `dev-change103p2-20260716145930`
> **對應 commit**: `7bf0111`（分支 `fix/change-103-azure-schema-drift` = `origin/main` a52d653 + schema-drift 修正）
> **前一個映像**: `dev-fix108-20260714135401`（= rollback 目標）
> **結果**: ✅ 部署成功，schema-drift 補上 CHANGE-103 P2 欄位，無事故

---

## 1. 部署內容（線上 7/14 dev-fix108 → 本次）

| 項目 | 說明 |
|------|------|
| CHANGE-103 P2a | Stage 1 公司配對四查詢加 `orderBy: createdAt asc`（決定性 tie-break） |
| CHANGE-103 P2 | token-set 公司配對（core-token 灰帶）+ `suspected_duplicate_of_id` 欄位 + PENDING 疑似重複審核 UI |
| FIX-111 | Stage 3 GLOBAL 提取 prompt 決定性選型（修 HKD 指示被旁路） |
| FIX-112 | 公司合併轉移 documents/extraction_results/rules（autoMergeCompanies + mergeCompanies） |
| FIX-113 | 存量孤兒合併資料回填盤點腳本（本地 + Azure 皆驗證 0 CORE 孤兒） |
| schema-drift 修正 | `apply-schema-drift.js` 加 CHANGE-103 P2 的 3 條冪等 DDL（本次部署阻塞的根因修正） |

---

## 2. 前置檢查（runbook §A.0）

| 檢查項 | 結果 |
|--------|------|
| 線上映像 | `dev-fix108-20260714135401`（7/14，commit ~91fa330） |
| `.env.example` diff（線上→目標） | **無新增 env**（GPT54 部署名已於 7/14 事故後補設，本次無 §16 類風險） |
| 新 migration（區間內） | **僅 1 支** `20260716113449_add_company_suspected_duplicate_of_id` |
| entrypoint 套用機制 | ⚠️ 不跑 `prisma migrate deploy`，只 bootstrap（空庫）+ gated `apply-schema-drift.js` → 非空庫需增量補欄位 |
| 一次性旗標安全檢查 | 全 `false`（`FORCE_SCHEMA_RESET=false` 確認非破壞性） |
| `docker-entrypoint.sh` CRLF | 0（§12）✅ |

> 🔴 **關鍵**：CHANGE-103 P2 只加了 Prisma migration，未加 `apply-schema-drift.js` 條目 → Azure 非空庫不會套用 → 新 Stage 1 讀 `suspectedDuplicateOfId` 會 P2022。部署前補上 3 條冪等 DDL 才避開。

---

## 3. 執行步驟

| # | 步驟 | 結果 |
|---|------|------|
| 1 | 建置映像 | `az acr build` → tag `dev-change103p2-20260716145930`（雲端 run `ck14` = **Succeeded**；本機串流照例在 `✔` cp1252 崩潰，以 `az acr task show-run` 判定） |
| 2 | 開 schema-drift | `RUN_SCHEMA_DRIFT_FIX=true` |
| 3 | 切換映像 | `az webapp config container set` → 新 tag |
| 4 | 重啟 | `az webapp restart`（exit 0） |
| 5 | 驗證健康 | `/api/health` → 200 + `{"database":"connected"}`，uptime 62s（新容器） |
| 6 | 驗證容器 log | schema-drift 6 applied / 0 failed（含 3 筆 CHANGE-103 P2）；`Step 3/3` → `✓ Ready in 1177ms`；無 P2022 |
| 7 | 收尾 | `RUN_SCHEMA_DRIFT_FIX=false` |

### 容器 log 關鍵行

```
[schema-drift] OK CHANGE-103 P2 column companies.suspected_duplicate_of_id
[schema-drift] OK CHANGE-103 P2 index companies_suspected_duplicate_of_id_idx
[schema-drift] OK CHANGE-103 P2 fk companies_suspected_duplicate_of_id_fkey
[schema-drift] done — 6 applied, 0 failed
[entrypoint] Step 3/3: starting Next.js server
 ✓ Ready in 1177ms
```

---

## 4. 一次性旗標

| 旗標 | 部署時 | 收尾 |
|------|--------|------|
| `RUN_SCHEMA_DRIFT_FIX` | `true`（套用 CHANGE-103 P2 欄位/索引/FK） | ✅ 設回 `false` |
| 其餘（`FORCE_SCHEMA_RESET` / `RUN_DEV_DATA_IMPORT` / `RUN_STAGE3_PROMPT_FIX` / …） | 全程 `false` | — |

> FIX-111 資料面（停用多餘 GLOBAL FIELD_EXTRACTION）、FIX-105（CEVA 合併）本 session 已於稍早經 Kudu ad-hoc 套用，本次部署不重跑。

---

## 5. 待驗證（使用者 UI）

- 重跑 CEVA 測試文件，確認：問題 1（HKD 取值，FIX-111）、問題 2（公司重複識別，CHANGE-103 P2a + token-set）效果。
- 疑似重複審核佇列頁 `/admin/companies/duplicate-review` 實際行為。

---

## 6. Rollback 路徑（本次未使用）

| 層級 | 操作 | 效果 |
|------|------|------|
| 完整回退 | 映像切回 `dev-fix108-20260714135401` + 重啟 | 整包回到 7/14 狀態（⚠️ 但 `suspected_duplicate_of_id` 欄位已加，向後相容、舊碼不讀故無害） |

---

*文件建立日期: 2026-07-16*
