# DEV 部署紀錄 — 2026-07-09（CHANGE-098 + SI-1~SI-6）

> **環境**: Azure DEV（App Service for Containers）
> **執行者**: AI 助手（Claude Code）+ 使用者
> **部署映像**: `dev-change098-20260709102634`
> **對應版本**: GitHub `main` `243d056`（PR #94）
> **依據流程**: [`dev-deployment-runbook.md §A`](../dev-deployment-runbook.md)（手動部署標準流程）

---

## 1. 目的

把 GitHub `main` 上的最新版本部署到公司 Azure DEV 環境，讓線上與最新程式碼一致。

本次新增內容（相對於部署前線上的 `dev-change095-20260706144044`）：

| 編號 | 內容 |
|------|------|
| CHANGE-098 | DB 連線韌性：`prisma.ts` pg Pool 加 `keepAlive`/timeouts/`max` + pool 錯誤監聽；`withDbRetry()` transient 重試；pipeline 對 transient DB 錯誤 fail-stop（不回退 V2、不白燒 GPT） |
| SI-1~SI-6 | 參考編號匯入修正：Sub Type 遺失修復、上限提高到 5000、批次可靠性（PR #93） |

兩批變更皆已透過 **PR #94**（squash commit `243d056`，2026-07-08 16:02 UTC）與 **PR #93**（`1875d53`）審查合併進 `main`。PR #91/#92 為本地 dev-tooling 腳本，不影響容器。

---

## 2. 部署方式

依專案既定規則，**只用手動部署**（無任何 GitHub Actions / 自動部署）：

```
az acr build → az webapp config container set → az webapp restart → 驗證
```

- 資源：RG `RG-RAPOSCM-AIDocProcessing-DEV`、WebApp `WebApp-RAPOSCM-AIDocProcessing-DEV`、ACR `acrscmdocprocessingdev`
- 平台：App Service for Containers（System-Assigned MI、VNet 整合到 infra RG、機密走 Key Vault 參考）

---

## 3. 執行步驟（實際）

| # | 步驟 | 內容 / 結果 |
|---|------|-------------|
| 0 | 前置查證 | 自上次部署（`8712488`/CHANGE-095）以來 **prisma schema/migration diff 全空**（純程式碼 + i18n）→ 一次性旗標全程 `false`；`docker-entrypoint.sh` 為 LF（CRLF=0）；az 以部署 SP 登入 rcitest 訂閱；本地 `main` 已同步 `243d056`、工作樹乾淨 |
| 1 | 建置映像 | `az acr build` → tag `dev-change098-20260709102634`（雲端 run `ckv` = **Succeeded**）。本機 log 串流照 runbook §11 的 cp1252（`✔`）崩潰 exit 1，屬**假失敗**；改以 `az acr task list-runs` 輪詢確認 Succeeded |
| 2 | 切換映像 | 先 `az acr repository show-tags` 確認新 tag 在 ACR 頂端 → `az webapp config container set`（exit 0） |
| 3 | 重啟 | `az webapp restart`（exit 0） |
| 4 | 驗證容器 | 抓 docker log（AAD bearer + Kudu，SCM 公開 IP `13.75.34.162` + `--resolve`）確認新容器啟動序列（見下） |
| 5 | 收尾 | 一次性旗標查證全為 `false`（`RUN_DEV_DATA_IMPORT` / `FORCE_SCHEMA_RESET` / `RUN_SCHEMA_DRIFT_FIX` / `RUN_STAGE3_PROMPT_FIX` / `RUN_EMAIL_VERIFIED_BACKFILL`）|

**新容器啟動序列（log 時間戳 `02:53Z`，AAD bearer + Kudu 抓取）**：
```
[entrypoint] Step 1/3: bootstrap database schema (if needed)
[bootstrap] public schema already has 122 tables -> skip init.sql
[entrypoint] Step 2/3: run essential seed (idempotent)   → roles/regions/cities/admin/settings 正常 upsert，✨ completed in 4.7s
[entrypoint] Step 3/3: starting Next.js server → ▲ Next.js 15.5.9 → ✓ Ready in 896ms
```
- **無** §12（exec 127 / CRLF）回歸、**無** §13（re2.wasm ENOENT）回歸。
- 唯一 `localStorage is not available ...` 為 Node 實驗性警告，無害、非回歸。

---

## 4. 出現的問題與處理

| # | 問題 | 根因 | 處理 | 是否影響部署 |
|---|------|------|------|-------------|
| 1 | `az acr build` 本機回 exit 1 | runbook §11 記載的 colorama/cp1252 崩潰（`✔` 字元編碼），**與建置無關** | 改看控制面 `az acr task list-runs`，確認雲端 run `ckv` = Succeeded | 否（無害） |
| 2 | 本機 `curl /api/health` 逾時 `HTTP=000`；`inboundIpAddress` 回空 | app host 有 `privatelink` CNAME、實際只走私有端點；本機在 VNet 外、公網無入站 VIP 可達（`publicNetworkAccess` 旗標雖為 Enabled 但無公開 VIP）| 依治理 §4，private 環境改走 **Kudu（AAD bearer）抓容器 log** 驗證，不從公網直測 health | 否（改用容器 log 驗證 Ready） |

> 註：本次無需登入 app / 無需 DB 資料操作，故未觸及 2026-07-06 記錄的 UI E2E 防火牆議題。

---

## 5. 已完成的結果

| 項目 | 結果 |
|------|------|
| Azure DEV 線上映像 | **`dev-change098-20260709102634`**（含 CHANGE-098 + SI-1~SI-6）|
| 與 GitHub `main` | 一致（`243d056`，PR #94）|
| 容器啟動 | entrypoint 三步驟走完、Next.js 15.5.9 `✓ Ready`、**無 §12/§13 回歸** |
| health 驗證 | 本機無法直測（app 走私有端點、在 VNet 外）；已以容器 log 的 `✓ Ready` + seed 成功佐證 |
| 一次性旗標 | 全程維持 `false`（無 schema 變更，未動用）|
| 回滾基準 | `dev-change095-20260706144044` |
| 本地 git | `main` 已同步 `243d056`、工作樹乾淨、已合併分支已清 |

**結論**：最新版本已成功部署到 Azure DEV 並正常啟動，部署本身完整驗證通過、無已知回歸。

---

## 6. 之後的建議 / 未完成項

1. **2026-07-08 01:42 UTC 受影響的 9 份文件重新匹配**：依使用者指示**本次暫不處理**。CHANGE-098 的韌性保護已隨此映像上線，之後重新匹配即受保護。
2. **內網 health / UI 驗證**：app 走私有端點，如需 `/api/health` 200 或 UI 行為驗證，須在公司內網 / VPN 進行。
3. **回滾方式**（如新版有問題）：
   ```
   az webapp config container set -g RG-RAPOSCM-AIDocProcessing-DEV -n WebApp-RAPOSCM-AIDocProcessing-DEV \
     --container-image-name acrscmdocprocessingdev.azurecr.io/ai-document-extraction:dev-change095-20260706144044
   az webapp restart -g RG-RAPOSCM-AIDocProcessing-DEV -n WebApp-RAPOSCM-AIDocProcessing-DEV
   ```

---

*紀錄建立: 2026-07-09 ・ 維護者: AI 助手*
