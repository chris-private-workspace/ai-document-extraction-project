# 2026-07-22 DEV 部署記錄（FIX-126~130 + CHANGE-106）

> **部署人**: AI 助手（使用者授權）
> **環境**: Azure DEV（App Service for Containers）
> **映像**: `acrscmdocprocessingdev.azurecr.io/ai-document-extraction:dev-fix126-130-ch106-20260722135255`
> **對應 commit**: `origin/main` @ `bc5adc1`（#136 合併後）
> **前一版映像**: `dev-fix121-125-20260721174308`（對應 `ef596c2` 基準）

---

## 本次帶上的變更

| 項目 | 內容 |
|------|------|
| FIX-126 | Stage 3 費用名稱比對強化（單複數歸一 + 方向閘 + 非對稱子字串） |
| FIX-127 | template mapping 前清除重複費用金額（金額指紋去重） |
| FIX-128 | mapping 公式死 key 可見化（執行時診斷 + UI 提示 + 儲存警告）＋ **schema：`template_instance_rows.transform_diagnostics`（nullable jsonb）** |
| FIX-129 | 公司合併跳過設定的介面回報（MergeSkippedReportAlert） |
| FIX-130 | 存量修正腳本 `prisma/apply-config-corrections.js`（資料修正已於部署前經 Kudu 完成 write，見 FIX-130 文件） |
| CHANGE-106 | template instance row 來源過期標記（staleSources） |

## 部署前檢查

| 檢查項 | 結果 |
|--------|------|
| 映像落後範圍（`ef596c2..origin/main`） | 僅本系列 3 個 commit（#128 / #134 / #136），無其他變更混入 ✅ |
| 工作樹內容 vs `origin/main` | `git diff` 為空、無未追蹤檔 ✅ |
| `src/` 新增 `process.env` 引用 | **0 個** ✅ |
| 新 schema 變更 | FIX-128 一欄（nullable、`add column if not exists` 冪等）→ 需 `RUN_SCHEMA_DRIFT_FIX=true` ✅ 條目已在 `apply-schema-drift.js` |
| 一次性旗標現值 | `FORCE_SCHEMA_RESET` / `RUN_DEV_DATA_IMPORT` / `RUN_EMAIL_VERIFIED_BACKFILL` / `RUN_STAGE3_PROMPT_FIX` / `RUN_TEMPLATE_MAPPING_SEED` 全 `false`、`RUN_AZURE_SYNC=inspect` ✅ |
| 資料安全評估 | bootstrap 只在空庫執行；essential seed 冪等 upsert；drift 只加欄位不刪不改 → **無資料遺失/覆蓋風險** ✅ |

## 執行步驟

| # | 步驟 | 結果 |
|---|------|------|
| 1 | `az acr build --no-logs`（控制面輪詢） | ✅ run `ck18` **Succeeded**（本機命令 exit 0，未遇 cp1252） |
| 2 | tag 就緒確認 | ✅ `dev-fix126-130-ch106-20260722135255` 於 ACR |
| 3 | `RUN_SCHEMA_DRIFT_FIX=true` | ✅ 獨立核驗現值 `true` |
| 4 | `az webapp config container set` → 新 tag | ✅ `container show` 核驗已切換 |
| 5 | `az webapp restart` | ✅ |
| 6 | `/api/health` | ✅ 200 `{"status":"healthy","services":{"database":"connected"}}`，uptime 62s |
| 7 | 容器 log 驗證 | ✅ `[schema-drift] OK FIX-128 column template_instance_rows.transform_diagnostics`、`done — 7 applied, 0 failed`、`[bootstrap] public schema already has 122 tables -> skip`、`✓ Ready in 743ms`；無 P2022 / exec not found |
| 8 | 旗標復位 `RUN_SCHEMA_DRIFT_FIX=false` | ✅ 獨立核驗 `false`；復位觸發的重啟後 health 再確認 |

## 容器 log 關鍵行

```
[entrypoint] Step 1/3: bootstrap database schema (if needed)
[bootstrap] public schema already has 122 tables -> skip init.sql
[entrypoint] (optional) applying schema drift fixes
[schema-drift] OK FIX-128 column template_instance_rows.transform_diagnostics
[schema-drift] done — 7 applied, 0 failed
[entrypoint] Step 2/3: run essential seed (idempotent)
[entrypoint] Step 3/3: starting Next.js server
 ✓ Ready in 743ms
```

## 回滾方式

```bash
az webapp config container set -g rg-raposcm-aidocprocessing-dev -n webapp-raposcm-aidocprocessing-dev \
  --container-image-name acrscmdocprocessingdev.azurecr.io/ai-document-extraction:dev-fix121-125-20260721174308
az webapp restart -g rg-raposcm-aidocprocessing-dev -n webapp-raposcm-aidocprocessing-dev
```

注意：`transform_diagnostics` 欄位為 nullable、舊代碼不讀取——回滾不需移除欄位。FIX-126 的比對變更為代碼內建（無 flag），回滾＝重部署舊映像即可。FIX-130 的資料修正獨立於映像版本（已 COMMIT 的 mapping/alias 修正對舊代碼同樣有效且正確）。

## 部署後待辦（FIX-130 驗收）

1. 重跑使用者回報的 17 份文件（Stage 3 以新邏輯處理）
2. 重跑相關 template instance
3. 核對 FIX-130 §驗收標準數值（`RIL_RCIM250015_14409` 的 `thc=325.42` 等）
4. FIX-130 §4/§5 業務判斷（Nippon 合併、SBS mapping 去留、Document vs Documentation Fee、RICON 併入 CEVA）
