# 2026-07-23 DEV 部署記錄（FIX-131 + FIX-132）

> **部署人**: AI 助手（使用者授權）
> **環境**: Azure DEV（App Service for Containers）
> **映像**: `acrscmdocprocessingdev.azurecr.io/ai-document-extraction:dev-fix131-132-20260723111721`
> **對應 commit**: `origin/main` @ `38fbd4e`（#140 合併後）
> **前一版映像**: `dev-fix126-130-ch106-20260722135255`（對應 `ddf2bac` 基準）

---

## 本次帶上的變更

| 項目 | 內容 |
|------|------|
| FIX-131 | 公司詳情頁補「合併公司」入口，讓兩間 ACTIVE 公司可透過 UI 合併（MergeCompanyButton + CompanyMergeDialog 型別放寬 + canManage gate + companies.json 三語言）。含實測揪出的 `forwarder.stats` undefined 崩潰修正 |
| FIX-132 | pg 連線池耗盡 P2028 修復：pool `max` 10→20、PrismaClient `transactionOptions`（maxWait 10s/timeout 20s）、template-matching `processBatch` 去每列 findFirst 縮短交易佔用、`batchMatch` 失敗不再靜默吞掉 |

## 部署前檢查

| 檢查項 | 結果 |
|--------|------|
| 映像落後範圍（`ddf2bac..origin/main`） | 僅本系列 3 個 commit（FIX-131 #138/#139 + FIX-132 #140），無其他變更混入 ✅ |
| 工作樹內容 vs `origin/main` | detached HEAD @ `38fbd4e`、`git status` 乾淨、無未追蹤檔 ✅ |
| `src/` 新增 `process.env` 引用（新 env） | **0 個**（FIX-131 純 UI；FIX-132 pool max/transactionOptions 皆硬編碼）✅ |
| 新 schema 變更 | **無**（`prisma/migrations/` 未動）→ 不需 schema-drift 條目、無 P2022 風險 ✅ |
| `scripts/docker-entrypoint.sh` 行尾 | LF（`.gitattributes` `text eol=lf` 保護）→ 避開 127 crash 雷 ✅ |
| 資料安全評估 | 無 schema 變更；bootstrap 只在空庫執行；essential seed 冪等 upsert → **無資料遺失/覆蓋風險** ✅ |

## 執行步驟

| # | 步驟 | 結果 |
|---|------|------|
| 1 | `az acr build` → tag `dev-fix131-132-20260723111721` | ✅ run `ck19` **Succeeded**（03:29:09 UTC）。本機命令 exit 1 = runbook 記載的 cp1252 log 串流崩潰（`✔` 於 `npx prisma generate`），**不影響雲端建置**，改控制面輪詢確認 |
| 2 | tag 就緒確認 | ✅ `dev-fix131-132-20260723111721` 於 ACR（`repository show-tags` 最新） |
| 3 | `az webapp config container set` → 新 tag | ✅ `container show` 核驗 `DOCKER_CUSTOM_IMAGE_NAME` 已切換 |
| 4 | `az webapp restart` | ✅ 04:00:05 UTC |
| 5 | app 健康輪詢 | ✅ HTTP 307（重導）@ 04:02:28 UTC（重啟後約 2.5 分鐘）|
| 6 | 容器 log 驗證 | ✅ bootstrap skip-init（122 tables）、essential seed 冪等 3.9s、`Ready in 1138ms`；無錯誤 / 無 crash / 無 P2028 |

## 容器 log 關鍵行

```
[entrypoint] Step 1/3: bootstrap database schema (if needed)
[bootstrap] public schema already has 122 tables -> skip init.sql
[entrypoint] Step 2/3: run essential seed (idempotent)
 Essential seed completed in 3.9s
[entrypoint] (optional) template field mapping seed: mode=false
  Ready in 1138ms
```

## 回滾方式

```bash
az webapp config container set -g RG-RAPOSCM-AIDocProcessing-DEV -n WebApp-RAPOSCM-AIDocProcessing-DEV \
  --container-image-name acrscmdocprocessingdev.azurecr.io/ai-document-extraction:dev-fix126-130-ch106-20260722135255
az webapp restart -g RG-RAPOSCM-AIDocProcessing-DEV -n WebApp-RAPOSCM-AIDocProcessing-DEV
```

注意：本次無 schema 變更、無新 env、無一次性旗標 → 回滾＝重部署舊映像即可，無需額外還原步驟。

## 部署後待辦（功能驗收，需使用者操作 — 涉及寫入真實資料）

1. **FIX-132 驗收**：重跑原本回報的 CEVA template instance 加 20 份文件場景 → 應正常匹配欄位；extraction 併發時文件列表不再 P2028（可請 AI 盯容器 log 確認）
2. **FIX-131 驗收**：對兩間 ACTIVE 重複公司（Nippon Express HK 兩筆）用詳情頁新「合併公司」按鈕執行合併，確認撞鍵報告（MergeSkippedReportAlert）正常
