# DEV 部署紀錄 — 2026-07-06（CHANGE-095 / FIX-102 / FIX-103）

> **環境**: Azure DEV（App Service for Containers）
> **執行者**: AI 助手（Claude Code）+ 使用者
> **部署映像**: `dev-change095-20260706144044`
> **對應版本**: GitHub `main` `8712488`（PR #90）
> **依據流程**: [`dev-deployment-runbook.md §A`](../dev-deployment-runbook.md)（手動部署標準流程）

---

## 1. 目的

把 GitHub `main` 上的最新版本部署到公司 Azure DEV 環境，讓線上與最新程式碼一致。

本次新增內容（相對於部署前線上的 `dev-fix101-20260630141327`）：

| 編號 | 內容 |
|------|------|
| CHANGE-095 | 允許在公司編輯頁補填空的 company code（僅補空值 + 稽核記錄） |
| FIX-102 | 修復公司建立/編輯的 FormData 契約不符導致的 500 |
| FIX-103 | 修正 companies 頁的 i18n（forwarder→company 殘留、new company 頁翻譯） |

三項變更皆已透過 **PR #90** 審查合併進 `main`（squash commit `8712488`，2026-07-06 06:24 UTC）。

---

## 2. 部署方式

依專案既定規則，**只用手動部署**（無任何 GitHub Actions / 自動部署）：

```
az acr build → az webapp config container set → az webapp restart → 驗證
```

- 資源：RG `RG-RAPOSCM-AIDocProcessing-DEV`、WebApp `WebApp-RAPOSCM-AIDocProcessing-DEV`、ACR `acrscmdocprocessingdev`
- 平台：App Service for Containers（PremiumV3 P1v3、System-Assigned MI、VNet 整合到 infra RG、機密走 Key Vault 參考）

---

## 3. 執行步驟（實際）

| # | 步驟 | 內容 / 結果 |
|---|------|-------------|
| 0 | 前置查證 | git 狀態乾淨；兩個 commit **無 Prisma schema 變更**（純程式碼 + i18n + 文檔）→ 一次性旗標全程維持 `false`；`docker-entrypoint.sh` 為 LF（CRLF=0）；az 以部署 SP 登入 rcitest 訂閱 |
| 1 | 同步 `main` | 本地 `main` 快進到 `8712488`（= PR #90），工作樹乾淨 |
| 2 | 建置映像 | `az acr build` → tag `dev-change095-20260706144044`（雲端 run `cku` = **Succeeded**，約 8 分鐘） |
| 3 | 切換映像 | `az webapp config container set` → 指向新 tag |
| 4 | 重啟 | `az webapp restart`（exit 0） |
| 5 | 驗證健康 | `/api/health` 連續穩定 **200** + `{"database":"connected"}` |
| 6 | 驗證容器 | 抓 docker log 確認新容器啟動序列（見下） |

**新容器啟動序列（log 時間戳 `06:59:55Z`，AAD bearer + Kudu 抓取）**：
```
[entrypoint] Step 1/3: bootstrap database schema (if needed)
[bootstrap] public schema already has 122 tables -> skip init.sql
[entrypoint] Step 2/3: run essential seed (idempotent)   → roles/regions/cities 正常 upsert
[entrypoint] Step 3/3 → Next.js 15.5.4 → ✓ Ready in 1631ms
```

---

## 4. 出現的問題與處理

| # | 問題 | 根因 | 處理 | 是否影響部署 |
|---|------|------|------|-------------|
| 1 | 一度誤判「PR #90 未 merged、commit 未進 GitHub」 | 本地 `origin/main` ref 過期（停在 `930c692`） | `git fetch` 後更正：PR #90 早已 merged 進 `main`（`8712488`） | 否（更正後正確部署） |
| 2 | `az acr build` 本機回 exit 1 | runbook §11 記載的 colorama/cp1252 崩潰（`✔` 字元編碼），**與建置無關** | 改看控制面 `az acr task list-runs`，確認雲端 run `cku` = Succeeded | 否（無害） |
| 3 | health 第一次 200 疑似舊容器 | 重啟切換有 downtime 窗口 | 再輪詢至**連續穩定 200**，並抓 log 確認新容器啟動序列 | 否（確認為新容器） |
| 4 | 抓容器 log 時 SCM host 連不到 | SCM host（`...scm...`）的內網 DNS 與 app host 分開，本機解析不到（`getaddrinfo failed` / HTTP 000） | 用公用 DNS 取 SCM 公開 IP + `curl --resolve` + `--retry-all-errors` 繞過；`/api/logs/docker` 回空改用 `/api/vfs/LogFiles/` | 否（僅診斷取得受阻） |
| 5 | UI E2E 登入無法穩定進行 | ①密碼因登入頁 hydration 未穩而 fill 掉字元（已修正驗證）②測試帳號密碼一度不符（帳號查證存在且正常，屬密碼問題）③**貴司防火牆間歇 403 攔截 app 頁面/RSC 請求**，導致 client 端白屏/跳 404 | 依使用者決定，UI E2E 改在**無防火牆環境獨立測試**；`/api/*` 不受防火牆影響 | 否（不影響部署本身；屬後續驗證） |

> 註：問題 5 的帳號查證是經使用者授權，在 VNet 內以 Kudu + `pg` 唯讀查詢，用完即清理殘留；未改動任何資料。

---

## 5. 已完成的結果

| 項目 | 結果 |
|------|------|
| Azure DEV 線上映像 | **`dev-change095-20260706144044`**（含 CHANGE-095 + FIX-102 + FIX-103） |
| 與 GitHub `main` | 一致（`8712488`，PR #90） |
| 健康檢查 | `/api/health` 穩定 200 + `database: connected` |
| 容器啟動 | entrypoint 三步驟走完、Next.js `✓ Ready`、**無 §12（exec 127/CRLF）/§13（re2.wasm ENOENT）回歸** |
| 一次性旗標 | 全程維持 `false`（無 schema 變更，未動用） |
| 回滾基準 | `dev-fix101-20260630141327` |
| 本地 git | `main` 已同步 `8712488`、工作樹乾淨 |
| 測試環境 | 已還原（Playwright 瀏覽器關閉、`.playwright-mcp/` 與暫存清除、容器診斷殘留清除） |

**結論**：最新版本已成功部署到 Azure DEV 並正常運行，部署本身完整驗證通過、無已知回歸。

---

## 6. 之後的建議

1. **UI 行為 E2E（未完成）**：CHANGE-095（補填空 code）與 FIX-102（建立/編輯不再 500）需登入後在 UI 驗證。因公司網路防火牆間歇 403 擋頁面路由，Playwright 在公司網路跑不穩，建議在**無防火牆環境/機器**手動或自動測。
2. **回滾方式**（如新版有問題）：
   ```
   az webapp config container set -g RG-RAPOSCM-AIDocProcessing-DEV -n WebApp-RAPOSCM-AIDocProcessing-DEV \
     --container-image-name acrscmdocprocessingdev.azurecr.io/ai-document-extraction:dev-fix101-20260630141327
   az webapp restart -g RG-RAPOSCM-AIDocProcessing-DEV -n WebApp-RAPOSCM-AIDocProcessing-DEV
   ```
3. **一次性旗標**：本次無 schema 變更，全程 `false`；日後若部署含 schema 變更的版本，才需依 runbook §14（`RUN_SCHEMA_DRIFT_FIX`）或 §11（`FORCE_SCHEMA_RESET`，破壞性）處理。

---

*紀錄建立: 2026-07-06 ・ 維護者: AI 助手*
